-- ============================================================
-- 通知機能: クライアント通知先メール + 監査ログ閲覧に必要な補助列
-- Supabase SQL Editor でそのまま実行可能
-- ============================================================

-- 商談FB格納時など、クライアント企業への通知を送る宛先。
-- 未設定の場合は通知をスキップする(必須項目にはしない)。
alter table companies
  add column if not exists notification_email text;
